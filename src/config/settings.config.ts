import { Settings } from './types';

export const APP_SETTINGS: Settings = {
  layout: 'demo6',
  container: 'fixed',
  layouts: {
    demo6: {
      headerSticky: false,
      headerStickyOffset: 0,
    },
  },
};
